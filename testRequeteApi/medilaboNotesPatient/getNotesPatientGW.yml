info:
  name: getNotesPatientGW
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/patient/note/8
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
